import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    marginLeft: '50%',
  },
  containerPrevisao: {
    backgroundColor: 'tomato',
  },
});
